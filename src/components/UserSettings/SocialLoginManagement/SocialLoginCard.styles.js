import styled from "styled-components/macro"

export const SocialLoginCardContainer = styled.div`
  border: 1px solid var(--gray75);
  padding: var(--spacing3);
  display: grid;
  gap: var(--spacing2);
`
